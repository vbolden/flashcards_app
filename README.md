# REFLECTION

- For me AI saved time with writing the HTML and CSS and picking a color scheme.

- I noticed that with the modal functions AI would use the showModal method, but it wasn't working in my code so I googled it and read that that method is used with the dialog element which I wasn't using. So I had to change it to the appendChild method instead. 

- I refactored the createBtn event listener to make sure the cards were displayed in the correct section of the page. 

- I added aria labels to buttons and added styling for focus elements

- I had to add even more detail to my prompts, instead of saying write a function for the newDeckBtn, I would have to say add an event listener with a function that creates a new deck when the newDeckBtn is clicked. I had to be a lot more specific than I originally thought. 